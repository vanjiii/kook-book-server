type Query {
  getUser(id: ID!): User
}

type Mutation {
  createUser(email: String!, password: String!): Message!
}

type User {
  id: ID
  name: String
  email: String
  avatar: String
}

type Message {
  Message: String!
}
